package com.example.threatiq;

import androidx.appcompat.app.AppCompatActivity;
import android.os.Bundle;

public class ResetPasswordActivity extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        // You'll need to create a corresponding layout file, e.g., 'activity_reset_password.xml'
        // setContentView(R.layout.activity_reset_password);

        // TODO: Add your password reset logic here
        // (e.g., an EditText for the email, a Button to send the reset link,
        // and Firebase Auth's sendPasswordResetEmail method)
    }
}
